<Window xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:vm="clr-namespace:Blob_Editor.ViewModels;assembly=Blob_Editor" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450" x:Class="Blob_Editor.Views.MainWindow" Icon="/Assets/avalonia-logo.ico" Title="Blob Editor">

    <Design.DataContext>
        <vm:MainWindowViewModel />
    </Design.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="12*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TabControl Grid.Row="0" Grid.Column="0">
            <TabItem Header="Properties" VerticalContentAlignment="Center">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="1.5*" />
                    </Grid.ColumnDefinitions>
                    <Panel Grid.Row="0" Grid.Column="0" Name="propertyButtons">
                        <Border Padding="4">
                            <StackPanel Orientation="Vertical">
                                <Button Width="60">Open</Button>
                                <Button Width="60">Save</Button>
                                <Button Width="60">Exit</Button>
                            </StackPanel>
                        </Border>
                    </Panel>
                </Grid>
            </TabItem>
            <TabItem Header="Bounding Box" VerticalContentAlignment="Center">
                <Grid ColumnDefinitions="100,1.5*,4*" RowDefinitions="Auto,Auto,Auto">
                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical">
                        <Button Width="80">Add point</Button>
                        <Button Width="80">Delete point</Button>
                    </StackPanel>
                    <TextBlock Text="{Binding Greeting}" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0" />
                    <CheckBox Content="Col2Row0" Grid.Row="0" Grid.Column="2" />
                    <Button Content="SpansCol1-2Row1-2" Grid.Row="1" Grid.Column="1" Grid.RowSpan="2" Grid.ColumnSpan="2" />
                </Grid>
            </TabItem>
        </TabControl>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0">
            <TextBlock Text="{Binding Status}" HorizontalAlignment="Left" VerticalAlignment="Center" />
        </StackPanel>

    </Grid>

    <Window.Styles>

        <Style Selector="Panel#propertyButtons">
            <Setter Property="Background" Value="#a59f9f" />
            <Setter Property="Margin" Value="0 -4 0 0" />
        </Style>

        <Style Selector="TabControl">
            <Setter Property="Background" Value="#F0F0F0" />
        </Style>

        <Style Selector="TabControl WrapPanel">
            <Setter Property="Background" Value="#2B579A" />
        </Style>

        <Style Selector="TabItem">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Height" Value="34" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Background" Value="#2B579A" />
            <Setter Property="Foreground" Value="#F0F0F0" />
            <Setter Property="Margin" Value="0 0 0 0" />
            <Setter Property="Padding" Value="10 0" />
        </Style>
        <Style Selector="TabItem:pointerover /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="#124078" />
        </Style>

        <Style Selector="TabItem:focus">
            <Setter Property="Foreground" Value="#2B579A" />
            <Setter Property="Margin" Value="0 0 0 0" />
            <Setter Property="Padding" Value="10 0" />
        </Style>
        <Style Selector="TabItem:focus /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="#f0f0f0" />
        </Style>

        <Style Selector="TabItem:selected">
            <Setter Property="Foreground" Value="#2B579A" />
            <Setter Property="Margin" Value="0 0 0 0" />
            <Setter Property="Padding" Value="10 0" />
        </Style>
        <Style Selector="TabItem:selected /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="#f0f0f0" />
        </Style>

    </Window.Styles>
</Window>
